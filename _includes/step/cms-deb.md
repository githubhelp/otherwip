#### Installing EmonCMS using the Debian package installer

    sudo apt-get --force-yes -y install emoncms
    
*note - {% include link/deb-list.md %} to be installed at the same time by adding package name to command above*
