#### Installing EmonHub with Debian package installer

    sudo apt-get update
    
    sudo apt-get --force-yes -y install emonHub
    
---------------------------------------------------------------
